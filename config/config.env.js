exports.mysql = {
    // 单数据库信息配置
    client: {
      // host
      host: '192.168.0.112',
    // port
    port: '3306',
    // username
    user: 'generate',
    // password
    password: 'generate',
    // database
    database: 'generate',    
    },
    // 是否加载到 app 上，默认开启
    app: true,
    // 是否加载到 agent 上，默认关闭
    agent: false,
  };